server:
  port: 8083
  
spring:
  application:
    name: CONFIG-SERVER
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Shradha2105/microservice-tutorial-config
          clone-on-start: true